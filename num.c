#include<stdio.h>
#include<conio.h>
#include<math.h>
void main()
{
int a;
scanf("%d",&a);
if(a<0)
printf("%d" is negative);
ifelse(a>0)
printf("%d" is positive);
else
printf("%d" is zero);
return 0;
}
